# MongoDB Connection String (use Atlas URI for cloud, or localhost for local)
DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/database?appName=Cluster0

# Payload CMS Secret (generate a random string for production)
PAYLOAD_SECRET=your-secret-key-here

# Optional: Frontend URL for CORS
FRONTEND_URL=https://idc-eta.vercel.app

# Optional: Server URL
SERVER_URL=http://localhost:3000
